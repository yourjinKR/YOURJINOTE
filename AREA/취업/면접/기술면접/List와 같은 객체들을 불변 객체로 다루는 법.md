`final` 키워드를 사용시 모든 객체가 불변 상태가 되는 것이 아닌 **재할당이 불가능**할 뿐입니다.  
`List`와 같은 컬렉션을 `final` 키워드를 사용시 **재할당만 불가능**할뿐 **삽입/삭제는 가능**합니다.  
이럴 경우에는 `Collections`의 `unmodifiableList()` 메서드를 사용하여 불변 리스트로 반환시킬 수 있습니다.