Java에서는 `private`, `default`, `protected`, `public`이 있습니다.  

- `private`는 클래스 내에서만 접근할 수 있다.
- `default`는 접근제어자를 별도로 지정하지 않은 상태이며 동일한 **패키지** 안에서만 사용할 수 있다.
- `protected`는 동일 패키지의 클래스 또는 해당 클래스를 **상속**받은 클래스 내에서 사용할 수 있다.
- `public`은 외부의 모든 클래스에서 전부 사용할 수 있다.