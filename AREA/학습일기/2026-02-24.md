
### issue1 : 새로고침시 기본 모드 노출 현상

```
원인 맞습니다. 
저장된 테마를 React useEffect에서 적용하다 보니 첫 페인트 후 테마가 바뀌는 FOUC가 발생합니다.  
해결은 layout에서 아주 이른 시점에 data-theme를 세팅하는 인라인 스크립트를 넣는 방식이 가장 안정적입니다. 적용 후 버튼 초기 상태도 함께 맞추겠습니다.
```

#### FOUC(Flash of Unstyled Content)와 FOUT(Flash of Unstyled Text)

> FOUC와 FOUT은 웹 페이지 로딩 과정에서 일시적으로 스타일이 적용되지 않은 콘텐츠와 텍스트를 사용자에게 보여주는 현상입니다. 두 현상 모두 스타일 로드 및 적용 시점과 브라우저 렌더링 시점 간의 차이 때문에 발생합니다.

아래와 같이 선실행 스크립트를 삽입하여 해결

```ts
const themeInitScript = `
(() => {
  try {
    const stored = window.localStorage.getItem("theme-preference");
    const root = document.documentElement;
    if (stored === "light" || stored === "dark") {
      root.setAttribute("data-theme", stored);
      return;
    }
    root.removeAttribute("data-theme");
  } catch (_) {}
})();
`;
```

```tsx
<head>
	<script dangerouslySetInnerHTML={{ __html: themeInitScript }} />
</head>
```

### issue2

![Pasted image 20260224152321](GALLERY/Pasted%20image%2020260224152321.png)

```
원인 분석:

layout.tsx (line 40)의 <html>은 서버에서 data-theme 없이 렌더됩니다.
그런데 <head>의 themeInitScript가 hydration 전에 document.documentElement.setAttribute("data-theme", ...)를 실행합니다.
즉, 서버 DOM과 클라이언트 초기 DOM 속성이 달라져 hydration mismatch 경고가 발생한 것입니다.
```

#### hydration이란?

서버로부터 받은 HTML에 React(js)를 입히는 것?




# 루틴 체크

```tasks
not done
```

# 완료

```tasks
done
done today
```
